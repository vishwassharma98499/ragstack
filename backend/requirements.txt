fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.2.1

# LangChain ecosystem
langchain==0.2.1
langchain-community==0.2.1
langchain-huggingface==0.0.3
langchain-ollama==0.1.1
langchain-groq==0.1.4

# Embeddings
# Note: do NOT pin torch with +cpu suffix — it doesn't exist on ARM (Apple Silicon).
# Let pip pick the right platform wheel automatically.
sentence-transformers==3.0.1
torch>=2.3.0
torchvision>=0.18.0

# PDF processing
pymupdf==1.24.3
pypdf==4.2.0

# Vector store
chromadb==0.5.0

# HTTP client
httpx==0.27.0

# Utilities
python-dotenv==1.0.1

# Authentication (Azure AD SSO)
PyJWT==2.8.0
cryptography==42.0.0
python-jose[cryptography]==3.3.0
pytest